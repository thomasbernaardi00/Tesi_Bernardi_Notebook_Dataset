# Tesi_Bernardi_Notebook_Dataset

In questo README spieghrò il contenuto di ogni file in questa carella

 FILE CSV DATEST:

-MASTER_DATASET_LLM_ART.csv = Questo file contiene tutte le opere analizzate dal secondo prompt per tutti i datest WikiArt, ArtQuest e WAGA e per tutti e 3 LLM(Gemini, ChatGPT, Claude)

 -wikiart_clean_final.csv = Questo file contiene tutte le opere analizzate dal secondo prompt per il dataset Wikiart per tutti e 3 gli LLM

 -artquest_clean_final.scv =  Questo file contiene tutte le opere analizzate dal secondo prompt per il dataset ArtQuest per tutti e 3 gli LLM

 -waga_clean_final.csv =  Questo file contiene tutte le opere analizzate dal secondo prompt per il dataset WAGA per tutti e 3 gli LLM

 -wiki_second_prompt_READY = Questo file contiene tutte le opere analizzate e filtrate dal primo prompt del dataset WikiArt

 -artquest_second_prompt_READY = Questo file contiene tutte le opere analizzate e filtrate dal primo prompt del dataset ArtQuest


NOTEBOOK (file ipynb):

-Dataset_WikiArt = in questo notebook viene analizzato il dataset wikiArt e sono eseguiti il PRIMO e il SECONDO PROMPT (SOLO PER GEMINI)

-Wiki_ART_secondo_Prompt_Chat_e_Claude = in questo notebook eseguiamo il SECONDO PROMPT nel datset WikiArt(PER CHATGPT E CLAUDE)

-PrimoESecondoPrompt_ArtQuest = in questo notebook analizziamo ed eseguiamo il PRIMO PROMPT(solo per gemini) e il SECONDO PROMPT(tutti e tre gli LLM) nel datset ArtQuest

wolfflin_WAGA =  in questo notebook anlizziamo ed eseguiamo il SECONDO PROMPT(con tutti e 3 gli LLM) per il dataset WAGA e valutiamo i risultati

Risultati_WikiArt_ArtQuest = in questo notebook analizziamo e valituiamo i risultati del SECONDO PROMPT per Artquest e WikiArt
